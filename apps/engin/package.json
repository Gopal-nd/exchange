{
  "name": "engin",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "redis": "^6.2.1"
  },
  "devDependencies": {
    "bun-types": "latest",
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*"
  },
  "module": "src/index.js"
}
